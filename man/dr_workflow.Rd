% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataraft-package.R
\name{dr_workflow}
\alias{dr_workflow}
\title{Define a modular product workflow or a delivery dependency graph}
\usage{
dr_workflow(..., inputs = list(), code_version = NULL, execution = NULL)
}
\arguments{
\item{...}{Named step functions. Their arguments must name dependencies;
defaults and \code{...} in step functions are not supported.}

\item{inputs}{Named list of initial input values.}

\item{code_version}{Explicit version of workflow code and dependencies.
Required for function dependency graphs; optional for modular workflows.}

\item{execution}{Optional connection-free \code{\link[dataraft.core:dr_execution_config]{dr_execution_config()}} for a modular
workflow. Function graphs configure execution inside their steps.}
}
\value{
A workflow specification. Modular workflows return ordinary product
run results from \code{\link[dataraft.core:dr_trial]{dr_trial()}}, \code{\link[dataraft.core:dr_run]{dr_run()}} or \code{\link[dataraft.core:dr_publish]{dr_publish()}}. Function dependency
graphs return named \code{results}, effective \code{inputs}, and a step \code{status} table.
}
\description{
Call \code{dr_workflow()} without step functions to start a modular assembly with
\code{\link[dataraft.core:dr_add_product]{dr_add_product()}}, \code{\link[dataraft.core:dr_add_recipe]{dr_add_recipe()}} and \code{\link[dataraft.core:dr_add_source]{dr_add_source()}}. Bind data at definition
time or with \code{dr_trial(flow, data = delivery)}. Use \code{\link[dataraft.core:dr_execution_config]{dr_execution_config()}} for
optional engine defaults. Each component can be extracted or replaced.
}
\details{
Named functions describe the receipt, preparation and dbt steps once.
Each function argument names an input or another step. Steps execute in
dependency order; a failed step blocks its consumers. Results remain ordinary
R values, so existing \code{\link[dataraft.lake:dr_ingest]{dataraft.lake::dr_ingest()}}, \code{\link[dataraft.core:dr_publish]{dr_publish()}} and dbt calls keep their meaning.
Keep report issuance outside the workflow as an explicit final decision.

On correction, pass replacement \code{inputs} and the \code{previous} workflow result
to \code{\link[dataraft.core:dr_run]{dr_run()}}. Unchanged successful branches are reused; changed inputs and their
consumers rerun. Failed branches retry on the next explicit call. Change
\code{code_version} whenever code, captured values or dependencies change. Use
\code{refresh} for named steps whose external sources changed without a new input.
This is sequential orchestration, not a scheduler or a distributed transaction.
Already committed steps stay committed if a later step fails.
}
\examples{
spec <- dr_product("orders") |> dr_add_quality(~ amount >= 0)
preparation <- dr_recipe() |> dr_step_mutate(amount = round(amount, 2))
flow <- dr_workflow() |> dr_add_product(spec) |> dr_add_recipe(preparation)
result <- dr_trial(flow, data = data.frame(amount = c(10.123, 20)))
dr_collect(result)
dr_extract_recipe(flow)
}
\seealso{
\code{\link[dataraft.core:dr_workflow]{dataraft.core::dr_workflow()}}
}
