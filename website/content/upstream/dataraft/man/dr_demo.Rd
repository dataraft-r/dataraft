% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demo.R
\name{dr_demo}
\alias{dr_demo}
\title{Try a checked insurance delivery in memory}
\usage{
dr_demo(quiet = FALSE)
}
\arguments{
\item{quiet}{Suppress the short status message.}
}
\value{
Invisibly, a list with \code{blocked} and \code{passed} trial results, and the
shared \code{workflow}. Use \code{\link[dataraft.core:dr_quality_rows]{dataraft.core::dr_quality_rows()}} to inspect the blocked result or
\code{\link[=dr_collect]{dr_collect()}} to collect the passed result.
}
\description{
Runs synthetic policy data through the same contract and quality gate twice.
A negative premium blocks the first delivery; the corrected delivery passes.
No database, files, network, credentials or optional engines are required.
}
\examples{
demo <- dr_demo()
demo$blocked$status
dr_collect(demo$passed)
}
